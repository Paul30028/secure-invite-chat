# 客户端默认后端（构建时注入）
# 中国区直连示例：
#   VITE_WS_URL=ws://192.168.1.8:8765
#   或机房公网 IP：
#   VITE_WS_URL=ws://x.x.x.x:8765
# 未来中继：
#   VITE_WS_URL=wss://chat.example.com

VITE_WS_URL=ws://127.0.0.1:8765

# 可选：写入邀请码的地址（与本机连接不同时使用）
# VITE_INVITE_WS_URL=ws://192.168.1.8:8765
