import type { ConnStatus } from "../hooks/useChatEngine";
import { APP_NAME } from "../config/appConfig";
export function Onboarding({ status, onConnectedSetup, onCreate, onJoin }: { status: ConnStatus; phoneHints?: string[]; onConnectedSetup: () => void; onOpenSettings: () => void; onCreate: () => void; onJoin: () => void }) {
 const ready = status === "online" || status === "local";
 return <div className="flex-1 flex flex-col justify-center bg-[#ededed] p-6"><div className="max-w-sm mx-auto w-full"><div className="w-14 h-14 rounded-2xl bg-[#07c160] grid place-items-center text-2xl mb-4">💬</div><h1 className="text-xl font-bold">{APP_NAME}</h1><p className="text-sm text-[#6b7280] mt-2">打开即可使用。应用会自动连接团队服务器，无需填写地址或端口。</p><div className={`mt-5 rounded-xl p-3 text-sm ${ready ? "bg-[#e5f6ee] text-[#118c43]" : "bg-[#fff8e8] text-[#805d1b]"}`}>{ready ? "已连接，可以开始聊天" : status === "connecting" ? "正在连接服务器…" : "暂时无法连接服务器"}</div>{!ready && <button className="w-full mt-3 py-3 rounded-xl bg-[#07c160] text-white" onClick={onConnectedSetup}>重新连接</button>}<div className="mt-5 space-y-3"><button disabled={!ready} className="w-full py-4 rounded-xl bg-[#07c160] text-white disabled:opacity-40" onClick={onCreate}>创建群聊 · 发邀请码</button><button disabled={!ready} className="w-full py-4 rounded-xl bg-white border border-[#d9d9d9] disabled:opacity-40" onClick={onJoin}>输入邀请码 · 加入</button></div></div></div>;
}
